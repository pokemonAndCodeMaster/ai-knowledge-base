---
id: "HUB-LQ"
title: "大模型数据生产与质检模块"
domain: ["llm_qa"]
type: "hub"

related_code: []
affects_path: []
trigger_keywords: ["llm_qa", "大模型", "LLM", "文本标签", "仿真"]
tags: []
summary: "大模型数据生产与质检业务域的总入口，聚焦 LLM 训练数据构造与文本标签管理。"
---

# 大模型数据生产与质检模块

大模型数据生产与质检业务域的总入口，聚焦 LLM 训练数据构造与文本标签管理。

> 📌 被引用为现状来源：[[质检一站式平台大模型质检模块整体架构]]（大模型质检 part 正向设计时引用本 Hub 作为生产链路现状来源）。

## 1. 核心业务流程

- [[仿真数据构造]] — Corner Case 仿真引擎生成→真值轨迹→触发生产→聚合打包

## 2. 数据表定义

- [[t_text_label_task 表]] — 文本/场景描述类标签任务表
- [[t_llm_task 表]] — LLM 任务调度核心表（6 通道 status + 派生任务级 status）
- [[t_llm_version_config 表]] — 版本配置表（通道配置/processor 动态 import）

## 2.1 任务调度模块

- [[TaskRepository 任务DB读写封装层]] — 全部 SQL 集中封装，含 `_sync_task_status()`
- [[TaskExecutor 任务生产执行器]] — 3 阶段 Pipeline (Stage1∥ → Stage2 → Stage3)，原 TaskExecutor 已合并入 scheduler.py
- [[TaskCreator 任务创建器]] — JSON/JSONL → INSERT，自动填充 obs_upload_status
- [[TaskQueryService 任务聚合查询]] — 概览/进度/吞吐统计
- [[DedupLockManager 去重锁管理器]] — 通道级去重锁（try_claim/complete/cleanup）
- [[DedupWatchdog 看门狗]] — 超时锁清理守护进程
- [[任务级vs通道级status架构设计]] — 核心架构决策：通道级原始→任务级派生

## 3. 底层工具箱

### 3.1 视频生产流水线

- [[VideoProduction 视频生产编排器]] — 视频生产主流程编排（autosenceid → 下载 → 生成视频 → 格式化 → 上传）
- [[VideoGenerator 视频生成器]] — 视频生成核心逻辑（单摄/多摄拼接）
- [[DataDownloader 数据下载器]] — autoscenes 数据下载（纯 ObsManager 管道 + parquet 解压）
- [[DatasetFormatter 数据集格式化器]] — ShareGPT/Qwen 多模态数据集 JSON 生成

### 3.2 LLM 推理流水线

- [[LLMInference LLM推理编排器]] — LLM 推理主流程编排（视频 → base64 → API调用 → 结果解析）

### 3.3 基础设施

- [[TimingCollector 时延收集器]] — 步骤级时延记录与统计量计算（count/min/max/avg/median/p95）
- [[LLMBaseError 异常体系]] — LLM 域三级异常（LLMBaseError → LLMConfigError / LLMAPIError）

### 3.4 配置数据模型

- `VideoConfig` / `VideoTask` / `VideoLayout` — 见 [[VideoConfig 视频配置模型]]
- `ExchangeRecord` — 见 [[ExchangeRecord 交换记录模型]]（端到端闭环数据载体）
- `DATASET_CFG` / `ACTIVATE_DATASET` — 遗留常量（从 video_gen/dataset_cfg.py 迁移，当前仅作兼容保留）
- [[LLM Tools 配置映射]] — application.yaml 中 llm_tools 配置段完整字段映射

### 3.5 示例脚本

- [[LLM Tools 示例脚本索引]] — 6个示例脚本的用法速查

### 3.6 pkl_vis 可视化引擎

- [[HUB-pkl_vis可视化模块]] — pkl 数据 + cam 图片 → MP4 可视化视频

> 跨域工具见 [[HUB-通用基建模块]]。

## 4. 护栏与红线

> 跨域护栏见 [[HUB-通用基建模块]]。本域遵循以下跨域护栏：

- [[全局单例 ConfigManager 使用规范]] — VideoProduction / LLMInference 均通过 get_nested() 读取配置
- [[Norm-OpenCV_Import_RROS_Path_Guard|OpenCV cv2 导入防护规范 ROS 环境路径冲突]] — VideoGenerator / DataDownloader 已实现 ROS 路径防护
- [[JSON 序列化性能陷阱：NumPy 类型泄漏的双层数据净化]] — DatasetFormatter.numpy_encoder 已遵循双层数据净化
- [[LLM Tools 开发规范与设计决策]] — llm_tools 域最高优先级规范（日志/配置/Layout/时间戳/黑帧/冗余类）
- [[LLM Tools 关键算法]] — composite帧拼合/时间戳修正/分位数统计三大算法

## 5. 通道解耦新架构 (production/)

- [[src_llm_production_通道生产包]] — 通道解耦新架构总览（7 通道 Producer + ABC 基类 + 注册表 + 状态轮询器）
- [[BaseProducer 通道生产基类]] — ABC 基类 + run_daemon 串行主循环 + _run_produce_on_task 步骤拆分
- [[TaskOrchestrator 任务级状态轮询器]] — 任务级 status 异步轮询派生（非进程编排器）
- [[WorkerGuardian Worker全生命周期守护器]] — Worker 注册/心跳/看门狗/退出清理（当前仅 scheduler.py 调用，production/ 守护盲区）
- [[通道解耦重构物理现状基线报告]] — ⭐ 通道解耦重构深度复盘物理现状基线（并存架构/并发退化/守护盲区）

## 6. 架构变迁

- [[src_llm 归一合并重构记录]] — task_scheduler + llm_tools + llm_api + xlsx_converter 合并为 src/llm/ 的完整映射

## 7. 包结构规范

- [[NORM-PACKAGE-INIT-极简化与循环依赖解除规范|NORM-PACKAGE-INIT-001 __init__.py 极简化规范与循环依赖解除模式]] — `src/llm/__init__.py` 与 `src/llm/production/__init__.py` 已按 A 流派极简化；`CHANNEL_REGISTRY` 抽到 `registry.py`，循环依赖由延迟 import 改为「字符串字典 + importlib 动态解析」解除。



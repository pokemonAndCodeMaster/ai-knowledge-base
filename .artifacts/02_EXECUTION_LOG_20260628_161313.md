# quality_check_pipeline 完整摄入执行日志

## 实际改动

- 刷新 `~/.notebooklm-mcp/auth.json` 后定位目标笔记本 ID。
- 冻结 38 个 source 清单；NotebookLM 纯文本 RPC 会压扁 Markdown，改用已登录 Windows Chrome 原始下载链。
- 导出 38 个原始 `.md` 到 `raw/notebooklm_exports/fc03a900-e886-44a5-85b0-73983c0efa41/`，生成 `_MANIFEST.md`。
- 对两篇长文生成 30 个 AST 语义分块。
- 写入/更新 36 张完整正文卡；source 2=15、9=17 合并编纂但原文分别保留。
- 新增 1 张 NotebookLM 来源总卡、1 张缺失引用索引、102 张防幻觉占位卡。
- 更新长期架构卡、前后端学习 Hub、`index.md`、`log.md`。

## 偏离计划

- 原计划用 MCP `source_export_all`，但 MCP 进程缓存了过期认证且 30 秒超时。
- NotebookLM 内容 RPC 可读但会丢失 Markdown 排版；为满足逐字节完整性，改用浏览器原始 `.md` 下载事件，并校验 `receivedBytes == totalBytes == 本地文件字节数`。
- 上游卡片引用 102 个未随 source 提供原文的条目；未伪造正文，改为 stale 占位卡与反向来源索引。

## 待验证

- 38/38 数量、哈希与正文嵌入对账。
- 新增卡片 Frontmatter 合法性、合法 domain/type。
- 本轮新卡断链为 0，图谱编译及过期检查。
- `index.md`、`log.md`、双向来源关系完整性。

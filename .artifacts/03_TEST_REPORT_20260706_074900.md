[ALL-PASS]

# 人工质检验收分配预览测试报告

## PostgreSQL、Service 与 HTTP

- `scripts/verify_acceptance_vertical_slice.sh`：通过。
- 固定结果：2 个任务、城区提交量 3500、3 个城区日期行。
- 显式任务选择：冻结 3 个日期单元，900 条计划量守恒，preview 落库并读回。
- FastAPI filtered selection：冻结全部 4 个日期单元，POST preview 后 GET round-trip 通过。
- Python：10 tests passed。
- `python -m compileall -q src`：通过。

## Vue

- `npm test -- --run`：3 files / 5 tests passed。
- 覆盖父子选择、半选、分析按需出现、预览主动生成、服务端结果展示、选择变化失效和 composable 选择签名。
- `npm run typecheck`：通过。
- `npm run build`：通过，126 modules transformed。
- `npm audit --audit-level=moderate`：0 vulnerabilities。

## 知识与代码质量

- `git diff --check`：通过。
- 图谱：384 张卡、1755 条链接、37 孤岛、145 断链；本轮增加 2 张已链接卡，孤岛和断链未增加。
- 代码过期检查仍报告 37 个历史参考项目问题；本轮三张相关代码卡 hash 均匹配。

## 未完成边界

- Group/Personal 策略、验收员池、供应商冲突、局部 override 和 execute 尚未实现。
- 真实 Delta 契约未知，“确认分配”保持禁用。

# quality_check_pipeline 完整摄入方案契约

## 目标

- 从 NotebookLM 笔记本 `quality_check_pipeline`（`fc03a900-e886-44a5-85b0-73983c0efa41`）导出当前全部 38 个 source。
- 原始 Markdown 逐字节保存，建立 `source_id`、字节数、SHA-256 清单，并用独立 RPC 对账。
- 对全部原文执行分块、全量大纲扫描和知识卡编纂，不遗漏规范、坑点、流程、模块关系与配置细节。
- 更新既有质检平台卡片、来源卡、综合卡、双向链接、`index.md`、`log.md`，最后重编译并检查图谱健康。

## 复用策略

- 复用 `wiki/quality_portal/` 现有 7 张卡片及 `wiki/synthesis/质检一站式平台长期架构.md`，避免制造重复卡。
- 原文保存在 `raw/notebooklm_exports/fc03a900-e886-44a5-85b0-73983c0efa41/`；wiki 只保存来源摘要和可复用知识。
- 具体参数、命令、配置和代码快照严格引用原文，不用概括替代。

## 文件级影响

- 新增：38 个原始 Markdown、完整性清单、摄入 Checklist、必要的来源/概念/规范/避坑/综合卡。
- 更新：`wiki/quality_portal/` 相关源头卡、`wiki/synthesis/质检一站式平台长期架构.md`、`index.md`、`log.md`。
- 禁止：修改参考项目目录；删除或覆盖无关用户内容。

## 验证计划

1. NotebookLM source 数 = 原始 Markdown 数 = 清单条目数 = 38。
2. 每个下载文件非空、UTF-8 可读、扩展名为 `.md`，SHA-256 唯一记录。
3. 独立 source-content RPC 的可见文本与原文件归一化文本逐源核验。
4. Checklist 与知识卡逐条对账，检查所有新卡 Frontmatter、索引和双链。
5. 运行 `python scripts/compile_graph.py`、`python scripts/check_staleness.py`，并按完成前验证 Skill 复核。

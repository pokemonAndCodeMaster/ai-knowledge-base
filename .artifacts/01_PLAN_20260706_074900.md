# 人工质检验收分配预览纵切计划

## 目标

在真实 Delta 未知时，完成“选择 → 服务端配额 → PostgreSQL 冻结 → 前端展示 → 选择变化失效”的端到端闭环。

## 交付

1. SelectionSpec 与 Ratio 规则 Schema；
2. 纯计算稳定配额；
3. preview PostgreSQL 写入和创建人读回；
4. preview POST/GET API；
5. Vue composable 与结果 panel；
6. PostgreSQL、HTTP、Python、Vitest、类型和构建验证；
7. 枢纽与关联知识卡同步。

## 非目标

- 真实或 fake Delta execute；
- Group/Personal 完整策略；
- 验收员池与供应商约束；
- preview 明细分页和局部 override。

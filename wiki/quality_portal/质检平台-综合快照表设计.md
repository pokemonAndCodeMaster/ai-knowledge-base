---
title: "质检平台-综合质检日常快照表设计(t_qc_daily_snapshot)"
domain: ["ai_dlc", "agent_evaluation", "tooling"]
type: "synthesis"
tags: ["quality_check_pipeline", "数据库设计", "验收", "快照表", "scene_name"]
created: 2026-06-28
updated: 2026-06-28
status: active
sources: 3
related_code: ["migrations/20260628_qc_daily_snapshot.sql", "task.md", "implementation_plan.md"]
affects_path: ["migrations/20260628_qc_daily_snapshot.sql", "src/manual_qc/"]
trigger_keywords: ["快照表", "t_qc_daily_snapshot", "stat_service", "scene_name", "验收统计", "标注进度", "验收进度", "option_annotation", "option_acceptance", "is_executed"]
---

# 质检平台综合日常快照表设计 (t_qc_daily_snapshot)

> 核心表：每行记录一名标注员在某日、某 scene、当日组别下的最小统计单元；既服务统计看板，也为采样配额和通过/打回判断提供计数依据。

← [[质检一站式平台人工质检模块整体架构]] | [[人工质检-Hub]] | [[人工质检-验收与通过打回]] | [[人工质检-人力管理体系设计]]

→ [[质检平台-Repository与数据库访问设计]] | [[质检平台-验收采样配额与任务选择设计]] | [[质检平台-通过打回规则与执行设计]]

> 第二轮复核：快照负责“算多少”和“统计结果”，任务级中间表/Delta 表负责“具体哪些 clip/task”以及执行后的真实状态。见 [[质检一站式平台Phase3前架构评审]]。

## 1. 设计原则

| 原则 | 说明 |
|------|------|
| **一行 = 一个最小统计单元** | `(stat_date × scene_name × group_name × annotator_id)`；annotator_id 必须有值 |
| **标注+验收合一** | 同一行承载该维度下标注进度、标注结果分布、验收分配、验收结果 |
| **执行结论同行** | 通过/打回结论和执行状态记录在同一行，无需跨表 JOIN |
| **采样计算依据** | sampler 从本表计算每个最小单元的配额；执行前再查任务级表取得 task_id/clip_id |
| **JSONB 无 schema 扩展** | 选项分布和验收明细用 JSONB，新增选项/题型无需加列 |

## 2. 维度设计

```
stat_date       DATE         -- 标注提交日期（以标注工作日为主轴）
scene_name      VARCHAR(256) -- 任务组/场景名（见 scene_name 概念卡片）
group_name      VARCHAR(128) -- 冗余存储的标注员组名（不做 FK，便于 GROUP BY）
annotator_id    INT → t_personnel.id  -- 必填，最小统计单元对应的标注员
```

本表只保存个人最小行，不额外保存组级汇总行。`group_name` 是历史维度快照：记录该标注员当天所属组，后续调组不回写历史。组级、scene 级和日期级结果通过 `GROUP BY` 直接聚合。

**唯一性保证**：`UNIQUE (stat_date, scene_name, group_name, annotator_id)`，只使用 PostgreSQL 10+ 支持的普通列唯一约束。

## 3. 字段分组设计

### 3.1 标注进度字段

```sql
annotation_total     INT   -- 该维度下分配的总标注任务数
annotation_submitted INT   -- 已提交/完成标注数
annotation_pending   INT   -- 待标注 = total - submitted
option_annotation    JSONB -- {"A": 120, "B": 30, "C": 15}  各选项标注数量
```

### 3.2 验收分配+进度字段

```sql
acceptance_allocated  INT  -- 状态回查后确认实际成功分配到验收员的数量
acceptance_submitted  INT  -- 已完成验收（验收员已判定）
-- 分配比例 = allocated / annotation_submitted，在应用层计算
```

### 3.3 验收结果字段

```sql
good_allocated  INT  -- 分配的 Good 类任务数
good_passed     INT  -- Good 类验收通过数
bad_allocated   INT  -- 分配的 Bad 类任务数
bad_passed      INT  -- Bad 类验收通过数

option_acceptance JSONB
-- {"A": {"allocated": 100, "passed": 96},
--  "B": {"allocated": 25,  "passed": 18},
--  "C": {"allocated": 12,  "passed": 9}}
-- key = 选项字母，value = {allocated, passed}
```

### 3.4 执行结论字段

```sql
conclusion       VARCHAR(16)  -- NULL / PENDING / PASS / REJECT
conclusion_basis JSONB        -- {"good_pass_rate": 0.96, "bad_pass_rate": 0.82, "rule": "RateBasedPassRule"}
confirmed_by     VARCHAR(64)  -- 确认人工号
confirmed_at     TIMESTAMPTZ

is_executed      BOOLEAN      -- FALSE = 未执行，TRUE = 已调用 Delta API 完成操作
executed_by      VARCHAR(64)
executed_at      TIMESTAMPTZ
execution_note   TEXT
```

## 4. 核心业务流程与数据流

```
① 标注完成 → StatService.compute_annotation_stats()
   → 读 Delta 原始表，按维度聚合标注数据
   → UPSERT t_qc_daily_snapshot（刷新 annotation_* 和 option_annotation 列）

② 验收分配 → AssignmentService.execute_assignment()
   → 执行采样（sampler 从本表读 option_annotation 做比例判断）
   → 根据配额查询任务级中间表/Delta 表，取得具体 task_ids
   → 调用 Delta API batch_assign()/batch_review_pass()
   → 回查任务状态后 UPSERT acceptance_allocated（实际成功量）

③ 验收进行中 → StatService.refresh_acceptance_stats()（定时或手动触发）
   → 读 Delta 原始表，获取最新验收状态
   → UPSERT t_qc_daily_snapshot（刷新 good_*/bad_*/option_acceptance 列）

④ 结论确认 → ExecutionService.confirm_conclusion()
   → 调用 PassRuleStrategy.evaluate(snapshot_row) 计算通过率
   → UPDATE conclusion / conclusion_basis / confirmed_by / confirmed_at

⑤ 结论执行 → ExecutionService.execute()
   → 调用 Delta API（batch_acceptance_pass 或 task_rollback×2）
   → UPDATE is_executed=TRUE / executed_by / executed_at
```

## 5. 采样引擎如何使用本表

采样计算阶段需要各最小单元的 Good/Bad 数量和按组/scene 分布；真正执行阶段才需要具体 clip/task。

```python
# sampler.py 中 RatioSampler.sample() 的伪逻辑
def calculate_quota(self, query_params: SamplingConfig) -> List[SamplingQuota]:
    # 从快照表读取最小单元计数
    snapshot = stat_repo.get_snapshot(date=query_params.date,
                                       scene_name=query_params.scene_name,
                                       group_name=query_params.group_name)
    option_dist = snapshot.option_annotation  # {"A": 120, "B": 30, ...}

    # 计算 Good(A) 和 Bad(B/C/D...) 分别要抽多少
    good_count = max(int(target * GOOD_RATIO), GOOD_MIN)
    bad_count  = max(int(target * BAD_RATIO),  BAD_MIN)

    return SamplingQuota(good_count=good_count, bad_count=bad_count, dimensions=...)

# AssignmentService 再按 quota 的维度从任务级表查询具体 task_ids
task_ids = task_repo.fetch_waiting_review_tasks(quota)
```

## 6. 通过率计算（应用层，不在 DB 算）

```python
# 在 StatService 或 ExecutionService 中
good_pass_rate = snapshot.good_passed / snapshot.good_allocated  if snapshot.good_allocated > 0 else None
bad_pass_rate  = snapshot.bad_passed  / snapshot.bad_allocated   if snapshot.bad_allocated  > 0 else None
total_pass_rate = (snapshot.good_passed + snapshot.bad_passed) / snapshot.acceptance_submitted

# 各选项通过率
for option, data in snapshot.option_acceptance.items():
    pass_rate = data['passed'] / data['allocated'] if data['allocated'] > 0 else None
```

## 7. 索引设计说明

| 索引 | 用途 |
|------|------|
| `(stat_date DESC)` | 按日期查最近数据 |
| `(scene_name, stat_date DESC)` | 按场景+日期筛选（最常见查询） |
| `(group_name, stat_date DESC)` | 按组筛选 |
| `(annotator_id, stat_date DESC)` | 按标注员筛选 |
| `(conclusion, is_executed)` | 查"有结论未执行"的待办项 |
| `(stat_date) WHERE conclusion IN ('PASS','REJECT') AND NOT is_executed` | 待执行看板专用 |

## 8. 执行顺序（Migration 依赖）

```
1. 20260628_personnel_and_permission.sql  ← t_personnel（被 t_qc_daily_snapshot.annotator_id 引用）
2. 20260628_qc_daily_snapshot.sql
```

> ⚠️ 护栏：`is_executed = FALSE` 时，不得绕过 ExecutionService 直接更新 conclusion；调用接口后必须通过任务状态回查校准真实结果。

> ⚠️ 护栏：sampler 用快照计算配额；Repository 只在执行准备阶段查询任务级表取得具体 task_ids，不重复计算统计口径。

# AGENTS.md

本文件是 Codex 进入本仓库后默认加载的项目级操作指南。请保持短小、可执行、可常驻上下文。详细工作流放在 `skills/`，项目知识放在 `wiki/`，原始资料放在 `raw/`，长规范沉淀为聚焦的 wiki 卡片。

## 项目使命

本项目是一个 AI 驱动的知识管理与代码开发一站式工作区。

核心思想：使用 Markdown 文档构建可维护的知识图谱，让项目知识、历史经验、踩坑记录、开发约定和代码地图持续复利。开发工作应从充分讨论需求开始，检索并复用知识库中的存量知识，基于端到端可运行结果完成交付，并在结束时把新增的可复用知识回写到知识库。

## 沟通约定

- 面向用户的回复优先使用中文。
- `wiki/`、`index.md`、`log.md` 中的新内容必须使用中文。
- 回答知识库事实时必须给出具体文件路径，不要把无出处的推测包装成项目事实。
- 需求不清时先讨论目标、约束、成功标准和端到端行为；小而安全的改动可以先做合理假设，并明示假设。
- 保持简洁，但不要省略关键取舍、风险和验证证据。

## 仓库地图

- `index.md`：知识库全局黄页，是人工浏览和知识路由入口。
- `schema.md`：wiki 结构、Frontmatter、命名、双链和演进规则。
- `TAXONOMY.yaml`：`domain`、`type`、`status` 枚举的权威来源。
- `log.md`：知识库操作的 append-only 时间线。
- `raw/`：只读原始资料区，禁止修改。
- `wiki/`：AI 维护的编译知识区。
- `wiki/sources/`：来源摘要卡。
- `wiki/entities/`：人物、组织、产品、项目等实体卡。
- `wiki/concepts/`：概念、规范、避坑、方法卡。
- `wiki/synthesis/`：跨来源综合分析和高层指南。
- `wiki/code/`：代码模块卡，必须通过 `related_code` 指向真实源码路径。
- `skills/ai-librarian/SKILL.md`：知识查询、摄入、代码摄入、健康检查的权威流程。
- `skills/verification-before-completion/SKILL.md`：完成前验证纪律。
- `skills/frontend-dev/SKILL.md`：前端开发规则。
- `demo_agents/`：旧版多 Agent 流程参考材料，用于迁移思想，不是当前 Codex 原生配置。
- `Yuxi/`、`agentscope/`、`codegraph/`、`gbrain/`、`graphify/`：用于学习和设计对标的开源参考项目。除非用户明确要求，不要修改这些目录。

## 知识优先原则

非平凡的设计、实现、重构、排障、评审开始前，必须先做聚焦的知识检索。

默认检索流程：

1. 如果 `.wiki_graph.json` 不存在，或刚刚更新过知识库，先运行 `python scripts/compile_graph.py`。
2. 运行 `python scripts/query_graph.py "<任务描述>"`。
3. 如果任务涉及具体路径，加 `--paths "路径1,路径2"`。
4. 如果领域明确，加 `--domains "knowledge_mgmt,agent_engineering"` 或其他 `TAXONOMY.yaml` 中的合法领域。
5. 优先读取返回结果中的 `full_read` 卡片；`summary_only` 用作上下文；`title_only` 只作为必要时的下钻候选。
6. 涉及代码时，先查 `wiki/code/` 和卡片中的 `related_code`，再读取聚焦源码。只有图谱信息不足时，才用 `rg` 定位符号和路径。

不要只凭记忆处理仓库中已有知识。不要捏造代码路径、API、约束、历史决策或模块关系。

## 知识库维护规则

详细规则以 `skills/ai-librarian/SKILL.md` 为准。根级硬规则如下：

- 禁止修改 `raw/`。
- `wiki/` 下卡片必须包含符合 `schema.md` 与 `TAXONOMY.yaml` 的 YAML Frontmatter。
- `domain` 必须是列表，`type` 必须是单值字符串。
- 使用 `[[双向链接]]` 建立知识关系。
- 优先写原子卡，不写巨型百科页；枢纽页只做导航和总览。
- `pitfall`、`norm`、`code_module` 必须通过 `affects_path` 或 `related_code` 绑定证据。
- 摄入资料时先列摄入清单，再写卡，最后按清单对账。
- 摄入代码时先运行 `python scripts/extract_code_skeleton.py <目标目录>`，不要直接大规模阅读源码。
- 知识变更后必须更新 `index.md`、追加 `log.md`，并运行 `python scripts/compile_graph.py`。
- 代码相关卡片可能过期时，运行 `python scripts/check_staleness.py` 或 `python scripts/check_staleness.py --code-only`。
- 发现旧路径、旧规范或旧术语时，不要静默迁就；应指出差异，并按当前仓库路径修正。

## 开发流水线

采用“足够严格但不过度繁琐”的流水线。旧版 `demo_agents/orchestrator.md` 的核心价值是：意图路由、PM 先调研、契约交付、实现、QA 验证、失败分诊、知识沉淀。Codex 中应保留这些关卡，但不要把所有任务都套进冗长仪式。

### 路线 A：纯知识查询

触发：用户只是在问架构、规范、历史经验、模块关系、资料总结，且不要求修改。

- 执行知识优先检索流程。
- 返回带文件路径出处的答案。
- 如果召回不足，说明缺口，并建议进一步查询、补卡或健康检查。

### 路线 B：敏捷运维 / 命令执行

触发：用户要求运行已有命令、脚本、查看状态或复现现象。

- 直接运行必要命令。
- 只总结关键输出、退出码和风险。
- 命令结果如果暴露出可复用经验，收尾时提醒是否需要沉淀到知识库。

### 路线 C：知识摄入 / 更新

触发：用户要求“加入知识库”“整理资料”“更新黄页”“沉淀经验”“检查笔记”。

- 使用 `skills/ai-librarian/SKILL.md` 的 `[ingest]`、`[query]`、`[health]` 或 `[compile]` 流程。
- 新知识必须和存量卡片建立双链，不要制造孤岛。
- 有冲突、过期、重复、断链时，列出来让人审视，不要擅自删除。

### 路线 D：标准开发 / 排障修复

触发：任何涉及新增功能、改代码、重构、修复报错、处理异常、架构调整的需求。

推荐状态机：

1. **需求与上下文发现**：澄清目标、约束、成功标准；运行知识检索；必要时读取相关 `wiki/code/` 和聚焦源码。
2. **方案与拍板**：对复杂或高风险任务，先给出方案摘要、文件级影响和验证策略，等待用户确认；小任务可在说明假设后直接执行。
3. **实现**：按既有模式做外科手术式修改，避免无关重构和新增复杂抽象。
4. **验证**：优先验证端到端用户路径或原始症状；必要时补充有价值的单元/集成测试。
5. **分诊返工**：验证失败时先分类。代码级错误回到实现；架构级缺陷回到方案；知识缺口回到知识检索或知识摄入。
6. **知识沉淀**：如果产生新规范、新坑点、新模块地图、新流程或旧知识失效，更新 wiki、`index.md`、`log.md` 并重编译图谱。

## 契约文件与上下文隔离

当任务复杂、跨多步、需要多 agent 或可能并发时，使用 `.artifacts/` 中的带 TAG 契约文件交接。TAG 建议使用秒级时间戳：`YYYYMMDD_HHmmss`。

- `.artifacts/01_PLAN_<TAG>.md`：方案契约。描述目标、复用策略、模块关系、文件清单、接口/函数契约和验证计划。复杂任务才需要落盘。
- `.artifacts/02_EXECUTION_LOG_<TAG>.md`：执行日志。记录实际改动、偏离计划之处、待验证点、待清理点。
- `.artifacts/03_TEST_REPORT_<TAG>.md`：验证报告。开头明确 `[ALL-PASS]` 或 `[FAIL]`，附关键命令、输出和失败堆栈。

简单任务不必创建契约文件，但仍必须保留“需求确认 -> 实现 -> 验证 -> 知识更新判断”的闭环。

## Subagent 使用原则

Codex 中只在确实有收益时使用 subagent。

- 探索型 subagent：适合并行查知识、扫模块、找风险。
- PM/架构型 subagent：适合复杂需求设计、方案对比、契约草案。
- QA/评审型 subagent：适合独立验证风险、回归、端到端路径和遗漏用例。
- 知识管理员型 subagent：适合最终知识摄入、双链修复、健康报告。
- Coder 型 subagent 不是默认必需；主会话已有足够上下文时，直接实现通常更高效。
- 不要让多个 agent 同时编辑同一批文件。
- subagent 报告只是输入，关键事实和完成状态必须由主会话复核。

## 验证标准

- 完成声明必须有新鲜验证证据。
- 行为改动优先验证端到端路径或原始故障症状。
- 单元测试只在能保护关键逻辑、边界条件或回归时添加；避免为了“看起来充分”而制造低价值用例。
- 如果不能运行验证命令，必须说明原因和剩余风险。
- 不要因为 lint 或单测通过就声称端到端成功，除非它们确实覆盖了用户可见路径。

## 质量标准

- 知识回答必须有出处。
- 代码变更必须聚焦且可验证。
- 重复错误、常见评审反馈、新约定应沉淀为 `AGENTS.md`、`schema.md`、skill 或 wiki 卡片。
- 新知识要进入图谱；旧知识失效要标记、更新或提出清理建议。
- 目标不是流程好看，而是知识可复用、开发可闭环、验证可信。

## 安全编辑

- 广泛编辑前查看 `git status --short`。
- 保留用户已有改动，不要回滚无关文件。
- 搜索优先使用 `rg` / `rg --files`。
- 手工编辑文件使用 `apply_patch`。
- 保持本文件简洁；规则变长时，迁移到 skill 或 wiki 卡片，并在此处保留入口。
